apiVersion: apps/v1
kind: Deployment
metadata:
  name: bh-core-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bh-core-app
  template:
    metadata:
      labels:
        app: bh-core-app
    spec:
      containers:
        - name: bh-core
          image: bh-core:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: bh-core-config
            - secretRef:
                name: bh-core-secret
